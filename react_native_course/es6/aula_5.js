// AMD
define(['./lib/jquery.js'], function($){
	$.ajax
});

// Common JS
// node_modules (padrão)
var express = require('express'); // nativo no node

// ECMAScript 6 (ES)
import React from 'react';
export myCustomModule;

